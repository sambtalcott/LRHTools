% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_reports.R
\name{render_reports}
\alias{render_reports}
\title{Render a batch of parameterized Quarto reports to PDF}
\usage{
render_reports(
  run_df,
  qmd_file,
  param_cols,
  file_col = "pdf_file",
  overwrite = FALSE,
  post_render = NULL,
  quiet = TRUE,
  ...
)
}
\arguments{
\item{run_df}{A data frame with one row per report. Must contain \code{file_col}
plus the columns named in \code{param_cols}.}

\item{qmd_file}{Path to the \code{.qmd} file}

\item{param_cols}{Character vector of \code{run_df} column names to pass as
quarto parameters. Optionally named to map a column to a differently
named quarto param, e.g. \code{c(id = "rn_id", "month")} passes the \code{rn_id}
column as param \code{id}}

\item{file_col}{Name of the column holding output PDF paths. Defaults to
\code{"pdf_file"}}

\item{overwrite}{Re-render reports whose PDF already exists? Defaults to
\code{FALSE}}

\item{post_render}{Optional function called with the row (a one-row tibble)
after each successful render, e.g. to copy figure files}

\item{quiet}{Suppress quarto rendering output? Defaults to \code{TRUE}}

\item{...}{Additional arguments passed to \code{\link[pagedown:chrome_print]{pagedown::chrome_print()}}}
}
\value{
The rendered subset of \code{run_df}, invisibly
}
\description{
Runs \code{\link[=render_qmd_pdf]{render_qmd_pdf()}} for each row of \code{run_df} with the standard
progress bar, skipping reports whose PDF already exists unless
\code{overwrite = TRUE}.
}
\details{
NOTE: chrome_print occasionally drops reports with handle_read_frame
errors. Run twice with \code{overwrite = FALSE} to ensure none are missed.
}
\examples{
\dontrun{
render_reports(run_df, "Prod/OPPE 03 Ind Report.qmd",
               param_cols = c("person", "quarter"))
}
}
