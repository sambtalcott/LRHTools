% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/m365.R
\name{lrh_m365_login}
\alias{lrh_m365_login}
\title{Warm up a Microsoft365R client using device-code auth}
\usage{
lrh_m365_login(fn_name, ...)
}
\arguments{
\item{fn_name}{Character. Name of the Microsoft365R function to warm up,
e.g. \code{"get_business_outlook"} or \code{"get_business_onedrive"}.}

\item{...}{Additional arguments forwarded to the resource function.}
}
\value{
The Microsoft365R client object, invisibly.
}
\description{
Pre-authenticates a Microsoft365R resource (Outlook, OneDrive, etc.) using
the device-code flow, and patches the underlying Microsoft365R function so
every later call in this R session returns the warmed-up client without
re-authenticating. Designed for unattended runs (e.g. the daily script)
where a browser pop-up cannot complete.
}
\details{
When device-code approval is required, the function emits a structured
marker line to the log:

\verb{! DEVICE_CODE_AUTH_REQUIRED url=https://login.microsoft.com/device code=ABC123}

The \verb{/dailyscript} slash command watches for this marker and pushes the URL
and code to the user's phone via PushNotification.

If a valid token is already cached on disk, no prompt fires.
}
