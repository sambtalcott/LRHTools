% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/render_reports.R
\name{render_qmd_pdf}
\alias{render_qmd_pdf}
\title{Render a parameterized Quarto report to PDF}
\usage{
render_qmd_pdf(qmd_file, pdf_file, params = list(), quiet = TRUE, ...)
}
\arguments{
\item{qmd_file}{Path to the \code{.qmd} file}

\item{pdf_file}{Output path for the PDF}

\item{params}{Named list of parameters passed to \code{execute_params}}

\item{quiet}{Suppress quarto rendering output? Defaults to \code{TRUE}}

\item{...}{Additional arguments passed to \code{\link[pagedown:chrome_print]{pagedown::chrome_print()}}, e.g.
\code{options = list(pageRanges = "1")} or \code{timeout = 60}}
}
\value{
\code{pdf_file}, invisibly
}
\description{
Renders a \code{.qmd} file with \code{quarto::quarto_render()} and then prints the
resulting sibling \code{.html} file to PDF with \code{pagedown::chrome_print()}.
This is the single-report unit used by the individual/department report
runner scripts (BCMA, OPPE, PG Unit, Trauma).
}
\details{
Because each render overwrites the html (and its figure files) from the
previous one, render + print must happen as a single step per report.
}
