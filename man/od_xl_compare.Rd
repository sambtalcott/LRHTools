% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/od_.R
\name{od_xl_compare}
\alias{od_xl_compare}
\title{Compare a dataframe to an existing spreadsheet}
\usage{
od_xl_compare(
  x,
  path,
  table = "Table1",
  id_cols,
  od = NULL,
  wb_types = NULL,
  coerce_tz = "America/New_York"
)
}
\arguments{
\item{x}{Data frame of rows to append (columns must match the table)}

\item{path}{The location in the Sharepoint drive}

\item{table}{Name of the Excel Table. Defaults to "Table1"}

\item{id_cols}{Column name (or vector of names) to use as an id.}

\item{od}{OneDrive (if null, will use the stored OneDrive)}

\item{wb_types}{Named integer vector passed to \code{\link[openxlsx2:wb_to_df]{openxlsx2::wb_to_df()}} as
its \code{types} parameter. Names are column names; values specify the target
R type:
\itemize{
\item \code{0} — character
\item \code{1} — numeric
\item \code{2} — Date
\item \code{3} — POSIXct (date-time)
\item \code{4} — logical
}

Example: \code{c(id = 1, name = 0, entered = 3)}. Columns not named are
auto-detected.}

\item{coerce_tz}{Timezone to \code{force_tz()} workbook POSIXct columns to.
Defaults to \code{"America/New_York"} to match the typical tz tagging of
data pulled from DuckDB. Only the workbook (\code{wb_df}) side is coerced;
if \code{x}'s POSIXct columns are not already in this tz, coerce them
yourself before calling. Set to \code{NULL} to skip coercion entirely.}
}
\value{
a list of (append, patch, remove) for use with \code{od_xl_append()},
\code{od_xl_patch()}, and \code{od_xl_remove()}. \code{remove} has all table columns plus
an \code{index} column (0-based row index within the table's data rows).
}
\description{
Returns a list of (append, patch, remove) for syncing an Excel Table to \code{x}.
\code{remove} contains rows present in the table but missing from \code{x} (matched by
\code{id_cols}) and can be passed to \code{\link[=od_xl_remove]{od_xl_remove()}} to delete them.
}
\section{Recommended application order}{
Apply the results in this order to avoid stale references:
\enumerate{
\item \code{\link[=od_xl_patch]{od_xl_patch()}} — uses absolute cell addresses captured at compare time,
so it must run before any operation that moves rows.
\item \code{\link[=od_xl_remove]{od_xl_remove()}} — row indices are still valid after patch.
\item \code{\link[=od_xl_append]{od_xl_append()}} — adding rows doesn't affect existing data-row positions.
\item \code{\link[=od_xl_sort]{od_xl_sort()}} — optional, last, once the row set is final.
}

Auto-coerces types in \code{x} that won't survive the compare:
\itemize{
\item factor columns become character
\item \code{difftime}/\code{hms}/\code{Duration} columns become numeric (seconds)
}

Auto-coerces date-times in path to ET

Auto-infers \code{wb_types} from \code{x}'s column classes (POSIXct=3, Date=2,
logical=4, numeric=1; character/unknown left to openxlsx2 auto-detect).
Entries in \code{wb_types} override the auto-inference for those columns, so
callers can correct a single odd column without re-listing the rest.
}

