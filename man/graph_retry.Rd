% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/od_.R
\name{graph_retry}
\alias{graph_retry}
\title{Retry a Microsoft Graph call on transient failures}
\usage{
graph_retry(
  .f,
  idempotent = TRUE,
  max_tries = 5L,
  base_wait = 1.5,
  max_wait = 32,
  quiet = FALSE
)
}
\arguments{
\item{.f}{A zero-argument function performing the Graph call, e.g.
\verb{\\() item$do_operation(...)}.}

\item{idempotent}{Is the wrapped call safe to repeat? \code{TRUE} (default) for
value-setting writes (PATCH a cell, apply sort, (un)protect) that yield the
same end state if re-run. Set \code{FALSE} for non-idempotent mutations (row
add / delete): those retry ONLY on statuses where the request provably
never reached the backend (408/429/503) — never on 500/502/504 or bare
connection errors, where the mutation may already have landed and a retry
would duplicate or mis-target it.}

\item{max_tries}{Maximum attempts including the first. Default 5.}

\item{base_wait}{Base seconds for backoff (\code{base_wait * 2^(try - 1)}, capped
at \code{max_wait}, plus up to \code{base_wait} of random jitter). Default 1.5.}

\item{max_wait}{Cap on a single backoff wait, in seconds. Default 32.}

\item{quiet}{Suppress the per-retry warning message.}
}
\value{
The value returned by \code{.f}.
}
\description{
Microsoft Graph intermittently returns transient HTTP errors (429 Too Many
Requests, 500, 502, 503, 504) or drops the connection, especially during
bursty per-cell / per-row workbook edits. Historically a single blip aborted
the whole calling script and forced a full re-run. This wraps a Graph call
and retries it with exponential backoff + jitter when the failure looks
transient; genuine errors (400/401/403/404/409, etc.) are re-thrown
immediately.
}
