% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_email.R
\name{html_highlight}
\alias{html_highlight}
\alias{html_highlight_change}
\alias{html_table_row}
\alias{html_kv_table}
\title{HTML fragments for notification emails}
\usage{
html_highlight(x, values = "--NOT DOCUMENTED--", color = "red")

html_highlight_change(old, new, values = "--NOT DOCUMENTED--", color = "red")

html_table_row(name, value)

html_kv_table(...)
}
\arguments{
\item{x, old, new}{Character vectors of display values}

\item{values}{Values to highlight. Defaults to \code{"--NOT DOCUMENTED--"}}

\item{color}{Highlight color. Defaults to \code{"red"}}

\item{name}{Row label (bolded in the left cell)}

\item{value}{Row value (right cell)}

\item{...}{For \code{html_kv_table()}: named character vectors, one per row of
the table, e.g. \code{html_kv_table("Unit" = unit, "DOB" = dob)}}
}
\value{
A character vector of HTML fragments
}
\description{
Helpers for building the per-record HTML tables used in tracker
notification emails (Falls, Restraints, Pressure Ulcers, ...).
}
\details{
\code{html_highlight()} bolds and colors values that need attention (by default
\code{"--NOT DOCUMENTED--"}). \code{html_highlight_change()} displays \code{"old -> new"}
when a value changed, highlighting either side as needed.
\code{html_table_row()} builds a single \verb{<tr>} key-value row, and
\code{html_kv_table()} builds a full bordered key-value table from named
arguments. All functions are vectorized, so they can be used directly
inside \code{mutate()}/\code{transmute()} to build one block per record.
}
\examples{
\dontrun{
new_rows |>
  mutate(across(where(is.character), ~html_highlight(replace_na(.x, "--NOT DOCUMENTED--")))) |>
  transmute(text = str_c(
    "### ", FIN, "  (", person_mrn, ")\n",
    html_kv_table("Unit" = enc_loc_nurse_unit, "DOB" = dob, "Injury" = injury)
  ))
}
}
