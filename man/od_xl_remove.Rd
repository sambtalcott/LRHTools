% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/od_.R
\name{od_xl_remove}
\alias{od_xl_remove}
\title{Remove rows from a named Excel Table}
\usage{
od_xl_remove(x, path, table, od = NULL, unprotect = FALSE)
}
\arguments{
\item{x}{Data frame with an \code{index} column (0-based). Extra columns are
ignored (so output of \code{od_xl_compare()$remove} works directly).}

\item{path}{The location in the Sharepoint drive}

\item{table}{Name of the Excel Table}

\item{od}{OneDrive (if null, will use the stored OneDrive)}

\item{unprotect}{If \code{TRUE}, temporarily unprotects the worksheet before
removing and re-protects afterwards. Only works with passwordless
protection.}
}
\value{
the ms_drive_item (invisibly)
}
\description{
Deletes rows from a named Excel Table by row index. Designed to work with
the \code{remove} output of \code{\link[=od_xl_compare]{od_xl_compare()}}, but can also be called directly
with a data frame containing an \code{index} column (0-based row indices within
the table's data rows).
}
\details{
Rows are deleted highest-index-first so that earlier deletions don't shift
the indices of pending ones.
}
