% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lrh_email.R
\name{lrh_email}
\alias{lrh_email}
\title{Send an email from the Quality Services mailbox}
\usage{
lrh_email(
  body,
  subject,
  to,
  cc = NULL,
  attachments = NULL,
  from = "QualityServiceDept@lrhcares.org",
  reply_to = NULL,
  send = TRUE
)
}
\arguments{
\item{body}{A markdown string for the email body}

\item{subject}{Subject line}

\item{to}{Character vector of recipient addresses}

\item{cc}{Optional character vector of cc addresses}

\item{attachments}{Optional character vector of file paths to attach}

\item{from}{Sending address. Defaults to the shared Quality Services
mailbox. Use \code{NULL} to send from your own account.}

\item{reply_to}{Optional reply-to address (the report emailers set this to
the shared mailbox so replies skip personal inboxes)}

\item{send}{Should the email be sent? If \code{FALSE}, the email is left as a
draft (useful for reviewing before a first live run).}
}
\value{
The \code{ms_outlook_email} object, invisibly
}
\description{
Wraps the Microsoft365R + blastula boilerplate used across the production
scripts: composes a markdown email, attaches any files, switches the sender
to the shared Quality Services mailbox, and sends.
}
\examples{
\dontrun{
lrh_email(
  body = str_glue("# Falls Tracking Updates\n[Falls Tracker]({link})"),
  subject = str_c(format(today(), "\%B \%e"), " Falls Tracking Updates"),
  to = fa_notify_emails
)
}
}
