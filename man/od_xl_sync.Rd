% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/od_.R
\name{od_xl_sync}
\alias{od_xl_sync}
\title{Sync a data frame to a named Excel Table on OneDrive}
\usage{
od_xl_sync(
  x,
  path,
  id_cols,
  od = NULL,
  table = NULL,
  sheet = NULL,
  remove = FALSE,
  unprotect = FALSE,
  wb_types = NULL,
  coerce_tz = "America/New_York",
  use_blocks = TRUE
)
}
\arguments{
\item{x}{Data frame to sync to the table}

\item{path}{The location in the Sharepoint drive}

\item{id_cols}{Column name (or vector of names) to use as an id}

\item{od}{OneDrive (if null, will use the stored OneDrive)}

\item{table}{Name of the Excel Table. \code{NULL} (default) resolves the table
from \code{sheet} — see \code{sheet} for the rules.}

\item{sheet}{Sheet name (or 1-based index) holding the table to sync, used
when \code{table} is not given. The sheet must contain exactly one Excel Table;
more than one is an error (name it with \code{table}). Defaults to \code{NULL},
which uses the table named \code{"Table1"} if the workbook has one, and
otherwise the single table on the first worksheet.}

\item{remove}{If \code{TRUE}, also remove rows present in the table but missing
from \code{x} (matched by \code{id_cols}). Default \code{FALSE} (additive sync — matches
the common case where the workbook retains history).}

\item{unprotect}{If \code{TRUE}, temporarily unprotects affected worksheets
before each operation and re-protects afterwards. Only works with
passwordless protection.}

\item{wb_types}{Forwarded to \code{\link[=od_xl_compare]{od_xl_compare()}}. Named list/vector of
openxlsx2 type codes overriding the auto-inferred types.}

\item{coerce_tz}{Forwarded to \code{\link[=od_xl_compare]{od_xl_compare()}}. Timezone to coerce
workbook POSIXct columns to. Default \code{"America/New_York"}; \code{NULL} to
skip.}

\item{use_blocks}{Forwarded to \code{\link[=od_xl_patch]{od_xl_patch()}}. \code{TRUE} (default) batches
contiguous changed cells into rectangular range writes; \code{FALSE} patches
one cell per request.}
}
\value{
The \code{\link[=od_xl_compare]{od_xl_compare()}} result invisibly (\code{list(append, patch, remove, table)}).
}
\description{
Convenience wrapper around \code{\link[=od_xl_compare]{od_xl_compare()}} + \code{\link[=od_xl_patch]{od_xl_patch()}} +
\code{\link[=od_xl_append]{od_xl_append()}} + (optionally) \code{\link[=od_xl_remove]{od_xl_remove()}}. Works while the file is
open in Excel, and on \code{.xlsm} as well as \code{.xlsx} — cells are edited in
place, so a macro-enabled workbook keeps its macros.
}
\details{
Rows are appended before any removal so that new rows inherit the Table's
per-row formatting — removing first can empty the Table down to its blank
placeholder row and lose that formatting.

Type coercion of \code{x} (factors → character, difftime/hms/Duration →
numeric), \code{wb_types} auto-inference, and \code{coerce_tz} handling are all
done by \code{\link[=od_xl_compare]{od_xl_compare()}} — see its docs for details.
}
