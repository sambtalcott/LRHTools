% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/od_.R
\name{od_xl_sync}
\alias{od_xl_sync}
\title{Sync a data frame to a named Excel Table on OneDrive}
\usage{
od_xl_sync(
  x,
  path,
  id_cols,
  od = NULL,
  table = "Table1",
  remove = FALSE,
  unprotect = FALSE,
  wb_types = NULL
)
}
\arguments{
\item{x}{Data frame to sync to the table}

\item{path}{The location in the Sharepoint drive}

\item{id_cols}{Column name (or vector of names) to use as an id}

\item{od}{OneDrive (if null, will use the stored OneDrive)}

\item{table}{Name of the Excel Table. Default \code{"Table1"}.}

\item{remove}{If \code{TRUE}, also remove rows present in the table but missing
from \code{x} (matched by \code{id_cols}). Default \code{FALSE} (additive sync — matches
the common case where the workbook retains history).}

\item{unprotect}{If \code{TRUE}, temporarily unprotects affected worksheets
before each operation and re-protects afterwards. Only works with
passwordless protection.}

\item{wb_types}{Named list/vector of openxlsx2 type codes that override the
auto-inferred types. Values: \code{0} (character), \code{1} (numeric), \code{2} (Date),
\code{3} (POSIXct), \code{4} (logical). Columns not named here use the auto-inferred
type from \code{x}.}
}
\value{
The \code{\link[=od_xl_compare]{od_xl_compare()}} result invisibly (\code{list(append, patch, remove)}).
}
\description{
Convenience wrapper around \code{\link[=od_xl_compare]{od_xl_compare()}} + \code{\link[=od_xl_patch]{od_xl_patch()}} +
(optionally) \code{\link[=od_xl_remove]{od_xl_remove()}} + \code{\link[=od_xl_append]{od_xl_append()}}. Works while the file is
open in Excel.
}
\details{
Auto-coerces types in \code{x} that won't survive the compare:
\itemize{
\item factor columns become character
\item \code{difftime}/\code{hms}/\code{Duration} columns become numeric (seconds)
}

Auto-infers \code{wb_types} from \code{x}'s column classes (POSIXct=3, Date=2,
logical=4, numeric=1; character/unknown left to openxlsx2 auto-detect).
Entries in \code{wb_types} override the auto-inference for those columns, so
callers can correct a single odd column without re-listing the rest.
}
